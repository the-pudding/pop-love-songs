import rawSongsData from "$data/20-EXPORT-viz-ready-data.json";

// Wrap each array in an object (so force simulation can mutate it)

// Calculate the radius form the popularity_score (NOTE: eventually we may need to update this based on screen size, but for now we're reduceing caluclations by doing it here)

// Export
export default rawSongsData;
